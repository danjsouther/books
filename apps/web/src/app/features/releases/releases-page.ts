import { Component } from '@angular/core';

/** The real release list (sharing `ReleaseStore` with the calendar) is Phase 7. */
@Component({
  selector: 'app-releases-page',
  imports: [],
  template: `<h1 class="text-2xl font-semibold">Releases</h1>`,
})
export class ReleasesPage {}
