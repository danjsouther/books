import { Component } from '@angular/core';

/** The real calendar (an `@angular/aria` Grid over `ReleaseStore`) is Phase 7. */
@Component({
  selector: 'app-calendar-page',
  imports: [],
  template: `<h1 class="text-2xl font-semibold">Calendar</h1>`,
})
export class CalendarPage {}
